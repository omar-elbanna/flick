# Backend API base URL (used by axios and Next rewrites)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Optional: WebSocket base (defaults to NEXT_PUBLIC_API_BASE_URL with ws scheme)
NEXT_PUBLIC_WS_BASE_URL=ws://localhost:8000
